/** we can implement dequeue assuming we are implementing two stacks are the both ends of the array
 * and while traversing we can simply start from *back till the end and then from 0 to *front
 */

#include <iostream>
#include <array>

using namespace std;

class deque{
    int front, back;


};
